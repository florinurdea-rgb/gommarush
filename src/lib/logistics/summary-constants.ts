/**
 * The Sumar dashboard's profit model for this MVP: a flat rate per tyre
 * actually delivered. One named constant, used everywhere profit is
 * computed — never a bare "* 2" scattered through the codebase.
 */
export const PROFIT_PER_DELIVERED_TYRE_EUR = 2;
